-picture
id
name
file
about
created
user

create table Picture (
    id bigint generated by default,
    name varchar(255),
    created timestamp,
    file varchar(255),
    about varchar(255),
    primary key (id)
);

create table PictureGroup (
    id bigint AUTO_INCREMENT,
    name varchar(255),
    description varchar(255),
    primary key (id)
);

create table GroupToPic (
    groupId bigint,
    picId bigint,
    primary key (groupId,picId)
);

create table GroupToPic (
    groupId bigint,
    memberOfGroupId bigint,
    primary key (groupId,memberOfGroupId)
);



-group
id
name
description

-groupToPic
groupId
picId

-groupMember
groupId
memberOfGroupId

-picureType
id
type
Size
TotalPrints

-pictureStock
id
pictureID
pictureType
stock
price



